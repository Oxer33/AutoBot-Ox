# ============================================
# AutoBot Ox - Dipendenze
# ============================================

# Core - Open Interpreter (il cuore del progetto)
# Nota: la versione su PyPI è 0.1.11 (ultima disponibile)
open-interpreter==0.1.11

# GUI - CustomTkinter (interfaccia moderna dark mode)
customtkinter==5.2.2

# Richieste HTTP (per health check e API calls)
requests==2.32.3

# Monitoraggio sistema (RAM, CPU)
psutil==6.1.1

# Computer Use - Controllo mouse e tastiera
pyautogui==0.9.54
pyperclip==1.11.0

# Packaging per creare l'EXE portable
pyinstaller==6.11.1

# --- Dipendenze future (non ancora usate nel codice) ---
# python-dotenv==1.0.1    # Per variabili ambiente (.env)
# cryptography==44.0.0    # Per crittografia API key
# Pillow==11.1.0          # Per gestione immagini
